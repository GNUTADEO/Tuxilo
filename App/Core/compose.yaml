services:
  api_core:
    image: Tuxhydro-Core
    build:
      context: .
      dockerfile: Dockerfile
      secrets:
        - github_token

secrets:
  github_token:
    file: ../Secrets/github_token
