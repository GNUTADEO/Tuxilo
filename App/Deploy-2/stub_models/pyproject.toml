[project]
name = "models"
version = "0.1.0"
description = "Stub models package for IIP"
requires-python = ">=3.10"
dependencies = [
    "sqlalchemy>=2.0.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
